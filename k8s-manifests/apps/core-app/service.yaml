apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: attestry
  labels:
    app.kubernetes.io/name: core
    app.kubernetes.io/part-of: attestry
spec:
  selector:
    app.kubernetes.io/name: core
  ports:
    - name: http
      port: 80
      targetPort: http
