apiVersion: v1
kind: Service
metadata:
  name: ledger
  namespace: attestry
  labels:
    app.kubernetes.io/name: ledger
    app.kubernetes.io/part-of: attestry
spec:
  selector:
    app.kubernetes.io/name: ledger
  ports:
    - name: http
      port: 80
      targetPort: http
